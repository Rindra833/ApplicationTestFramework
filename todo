FRAMEWORK:

Framework/
│
├── src/
│   └── mg/
│       └── framework/
│           └── FrontServlet.java
│
├── build/
│
├── lib/
│   └── servlet-api.jar
│
├── build-framework.sh
│
└── framework.jar

APPLICATION_TEST

ApplicationTest/
│
├── src/
│
├── src/main/
│   └── webapp/
│       ├── index.jsp
│       │
│       └── WEB-INF/
│           ├── web.xml
│           └── lib/
│               └── framework.jar
│
├── build/
│
└── deploy.sh

Suite:

OBJECTIF: Spring MVC & Web API

Spring MVC: Controller (annoté) 
- Prerequis :
- Creer une Annotation (Dans ApplicationTest) (à apprendre par nous meme) : creer - recuperer
-> Creer class -> tester(quelle annotation)
- Appliquer dans : classe/methode/attribut 
- variables(attributs) à mettre dans Annotation

*D ici Mardi 16 Juin* 


Sprint 01 :

- framework : class Annotation (Creer class Controller(testController -> deploy sur Tomcat))
Voir tous class path |ou| dev fournit nom package
Dans web.xml (on peut donner nom de variables (package ou il y a class Controller))) 
Annotation -> .jar -> path
- [code] : 
            . Demarrage Appli Web
            . listener
            . 1er appel Front Servlet (fonction init())
- OBJECTIFS : Avoir tous les Controller
- F.S. Controller -> init -> parcours (test class) -> oui -> fullName -> ajout à la listController :  
            . Attributs: liste Chaine de caractere, liste Controller
- Methode pour savoir s`il exixte Annotation 